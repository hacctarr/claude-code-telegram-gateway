# Secrets & machine-local state — NEVER commit these
config.json
links.json
ignored.json
superseded.json
sessions.json
.gateway.lock
gateway.log
*.log

# Dependencies (none required, but just in case)
node_modules/

# macOS
.DS_Store
restart.flag
